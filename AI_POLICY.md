# 오늘밤! BAR AI 엔진 운영 지침 (AI_POLICY.md)

본 지침은 '오늘밤! BAR' 애플리케이션의 AI 분석 및 데이터 처리 로직의 핵심 원칙을 규정한다.

## 1. 데이터 격리 및 초기화 (Stateless Processing)
- 모든 포스터 분석 요청은 독립적으로 처리한다.
- 새 이미지 업로드 시 기존 메모리(date, location, title, time, fee, region)를 즉시 `null` 또는 공백으로 초기화한다.
- 이전 요청의 데이터를 현재 분석에 절대로 결합하거나 참조하지 않는다.

## 2. 17개 광역 지자체 분류 엔진 (Geographic Classification)
- 17개 광역 지자체(시·도) 리스트를 최우선 순위로 매칭한다.
- **1순위**: 전체 주소 매칭 (예: '인천광역시', '인천 미추홀구' -> 인천 확정)
- **2순위**: 핵심 키워드 매핑 (예: '송도', '구월', '석바위' -> 인천 / '강남', '홍대', '이태원' -> 서울)
- **3순위 (Fallback)**: 판별 불가 시 임의 배정 금지. 사용자에게 선택 요청.

## 3. 시각 데이터 추출 규칙 (OCR Optimization)
- **날짜/시간**: 가장 큰 폰트와 요일/PM/AM 키워드를 우선순위로 추출한다.
- **참여 회비**: '원', 'KRW', '￦' 표시 숫자를 추출하며, 필드명은 반드시 **'참여 회비'**로 통일한다.

## 4. 도배 방지 및 데이터 병합 (Upsert Logic)
- 분석 결과가 기존 DB의 **[날짜] + [장소] + [시간]**과 90% 이상 일치할 경우, `UPDATE`를 수행하여 포스터 이미지를 최신으로 교체한다.

## 5. 사용자 피드백 루프 (Confidence Check)
- 확신도가 70% 미만인 필드는 강제로 비워두고, 시각적 포커스를 주어 사용자의 검증을 유도한다.
